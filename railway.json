{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/api/Dockerfile"
  },
  "deploy": {
    "startCommand": "/app/apps/api/start.sh",
    "healthcheckPath": "/health",
    "restartPolicyType": "ALWAYS"
  }
}
